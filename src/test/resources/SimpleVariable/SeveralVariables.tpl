Hello ${name}, this is a test.
${names} it's not the same variable
${_name} also does not.
The variables are case sensitive, ${Name} is different from ${name}

${name}, ${occupation}, ${age} years old.

Predefined variables

- now: ${now}
